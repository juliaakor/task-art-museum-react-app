import { styled } from 'styled-components';

export const SearchWrapper = styled.div`
  margin-bottom: var(--c-spacing-xxx-large);
`;

export const CardListWrapper = styled.div`
  width: fit-content;
  margin: auto;
`;
